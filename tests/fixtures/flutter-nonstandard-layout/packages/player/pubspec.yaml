name: player
description: A standalone player package in a non-standard layout.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.0
  url_launcher: ^6.0.0
  sentry_flutter: ^7.0.0

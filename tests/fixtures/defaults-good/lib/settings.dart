class Settings {
  final shareData = Setting(key: 'share', defaultValue: false);
  final analytics = Setting(key: 'analytics', defaultValue: false);
  final darkMode = Setting(key: 'darkMode', defaultValue: true);
}
